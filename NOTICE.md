# Notice

This repository is a public portfolio extract from internal Octadecimal agent-engineering experiments.

The extraction preserves selected Git history for the adapter layer while removing private operational context. Some adapters reference third-party frameworks by name, but this repository does not vendor those frameworks. Install dependencies from each framework's official distribution and observe their licenses.
